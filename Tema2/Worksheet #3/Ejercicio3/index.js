var Cadena = "Limpiaparabrisas"
var Aparicion = "para";

function encontrarTodasLasApariciones(Cadena,Aparicion)
{
    var Posicionletra;
    var Posiciones;
    for(var i=0; i <= Cadena.lenght -1;i++)
    {
        var Posicionletra;
        for(var j=0; j <= Aparicion.lenght -1;j++)
        {
            
        }
    }
}